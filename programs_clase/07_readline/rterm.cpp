#include <stdio.h>
#include <stdlib.h>
#include <readline/readline.h>

int main(){

    char *linea;

    system("toilet -f pagga -F border --gay 'Remote computer'");
    printf("Hack me if you can.\n\n\n");

    linea = readline("Login: ");    //con ésto leemos la línea
    printf("%s isn't a valid passwd.\n", linea);
    linea = readline("Login: ");    //con ésto leemos la línea
    printf("%s isn't a valid passwd.\n", linea);
    linea = readline("Login: ");    //con ésto leemos la línea
    printf("%s isn't a valid passwd.\n", linea);


        free(linea);





	return EXIT_SUCCESS;
}
